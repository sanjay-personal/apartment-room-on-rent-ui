import { AllowonlynumbersDirective } from './allowonlynumbers.directive';

describe('AllowonlynumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowonlynumbersDirective();
    expect(directive).toBeTruthy();
  });
});
